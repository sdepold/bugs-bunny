h1= title

h2 Possible bugs bunnies:

ul
  - for(var address in mailAddresses) {
    - var username = mailAddresses[address]
    - var isCurrentBugsBunny = (address == currentBugsBunny.address)

    li= (isCurrentBugsBunny ? '=> ' : '') + username + ' (' + address + ')'
  - }

h2 Current bugs bunny:

ul
  li= currentBugsBunny.name + ' (' + currentBugsBunny.address + ')'

h2 Additional notified people:

ul
  - for(var address in notifications) {
    - var username = notifications[address]

    li= username + ' (' + address + ')'
  - }
